import nodes

class LoadImageClipboardNode(nodes.LoadImage):
    CATEGORY = "🚀🌍 AstroCorp 🌍🚀"

NODE_CLASS_MAPPINGS = {
    "LoadImageClipboardNode": LoadImageClipboardNode,
}

NODE_DISPLAY_NAME_MAPPINGS = {
    "LoadImageClipboardNode": "Load Image Clipboard",
}
